{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceRoot}/target/debug/hello",
            "args": [],
            "cwd": "${workspaceRoot}"
        }
    ]
}